<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= title || '404 — Tidak ditemukan' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-indigo-50 via-white to-pink-50 flex items-center justify-center p-6">
  <div class="relative max-w-xl w-full">
    <!-- blobs -->
    <div class="absolute -top-10 -left-10 w-40 h-40 bg-indigo-300/30 rounded-full blur-3xl"></div>
    <div class="absolute -bottom-10 -right-10 w-44 h-44 bg-pink-300/30 rounded-full blur-3xl"></div>

    <div class="relative bg-white/90 backdrop-blur rounded-2xl shadow-xl border border-slate-100 px-6 py-8">
      <div class="text-center">
        <div class="text-7xl font-extrabold tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-pink-600">
          404
        </div>
        <h1 class="mt-2 text-2xl font-bold text-slate-900">Halaman tidak ditemukan</h1>
        <% if (typeof url !== 'undefined' && url) { %>
          <p class="mt-1 text-sm text-slate-500">URL: <code class="px-1.5 py-0.5 bg-slate-100 rounded"><%= url %></code></p>
        <% } %>
        <p class="mt-3 text-slate-600">Coba periksa kembali alamatnya, atau kembali ke halaman utama.</p>

        <div class="mt-6 flex flex-wrap items-center justify-center gap-3">
          <a href="/" class="px-4 py-2 rounded-xl bg-indigo-600 text-white hover:bg-indigo-700 shadow">
            Ke Beranda
          </a>
          <% if (typeof admin !== 'undefined' && admin) { %>
            <a href="/admin/dashboard" class="px-4 py-2 rounded-xl bg-white border border-slate-200 text-slate-800 hover:bg-slate-50">
              Dashboard Admin
            </a>
          <% } else { %>
            <a href="/admin/login" class="px-4 py-2 rounded-xl bg-white border border-slate-200 text-slate-800 hover:bg-slate-50">
              Login Admin
            </a>
          <% } %>
          <a href="javascript:history.back()" class="px-4 py-2 rounded-xl text-slate-600 hover:bg-slate-100">
            Kembali
          </a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
